/**
 * AI Provider Abstraction — Section 6.4
 *
 * Normalizes the interface across Anthropic, OpenAI, and Google SDKs.
 * Full implementation in Phase 4 (AI Integration).
 *
 * Exports:
 * - AIProvider interface
 * - AnthropicProvider
 * - OpenAIProvider
 * - GoogleProvider
 * - getProvider(name: string): AIProvider
 */

export {};
